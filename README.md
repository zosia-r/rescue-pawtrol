# rescue-pawtrol
Advanced Web Technologies project

## Docker

Run the full stack with:

```bash
docker compose up --build
```

The frontend is available at `http://localhost`, the backend at `http://localhost:8080`, and PostgreSQL at `localhost:5434`.
